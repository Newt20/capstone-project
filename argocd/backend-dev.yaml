apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: backend-dev
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Newt20/capstone-project.git
    targetRevision: main
    path: k8s/dev/backend
  destination:
    server: https://kubernetes.default.svc
    namespace: support-chat-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
