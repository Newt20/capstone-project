apiVersion: v1
kind: Service
metadata:
  name: frontend-dev
  namespace: support-chat-dev
  labels:
    app: support-chat
    component: frontend
    env: dev
spec:
  type: ClusterIP
  selector:
    component: frontend
    env: dev
  ports:
    - name: http
      port: 80
      targetPort: 5173
