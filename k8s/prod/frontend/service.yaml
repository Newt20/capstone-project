apiVersion: v1
kind: Service
metadata:
  name: frontend-prod
  namespace: support-chat-prod
  labels:
    app: support-chat
    component: frontend
    env: prod
spec:
  type: ClusterIP
  selector:
    component: frontend
    env: prod
  ports:
    - name: http
      port: 80
      targetPort: 80
