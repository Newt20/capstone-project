apiVersion: v1
kind: Service
metadata:
  name: frontend-stage
  namespace: support-chat-stage
  labels:
    app: support-chat
    component: frontend
    env: stage
spec:
  type: ClusterIP
  selector:
    component: frontend
    env: stage
  ports:
    - name: http
      port: 80
      targetPort: 80
